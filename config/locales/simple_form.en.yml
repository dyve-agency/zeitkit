en:
  simple_form:
    required:
      text: 'required'
      mark: '*'
    labels:
      worklog:
        summary: "What did you do?"
      session:
        username: "Email"
      invoice:
        note: Footer notes (Markdown)
        number: Invoice number
        content: Invoice content (Markdown)
        payment_info: Payment info (Markdown)
        payment_terms: Payment terms (Markdown)
        vat: VAT
        discount: Discount Absolute
      invoice_default:
        content: Invoice content (Markdown)
        payment_info: Payment info (Markdown)
        payment_terms: Payment terms (Markdown)
        vat: VAT
      client:
        credit_block_reason: Reason for why work is blocked. Shown to team.
      product:
        charge: Surcharge in %
    hints:
      worklog:
        hourly_rate: Set a custom hourly rate for i.e. rush jobs.
        paid: This worklog has been paid.
      expense:
        reason: Make sure to keep receipts in case the client want more details on this expense.
      product:
        charge: Allows you to add a surcharge to the product price for your client. I.e. when you are reselling a server.
      invoice:
        discount: This value will be substracted from the invoice total.
      client:
        credit_block_reason: When a client is late on payments this can be used to show an alert to your co-workers. When they are creating a worklog they will see an alert that the credit is blocked. This is useful to avoid unpaid invoices. Once your client paid, simply remove the text from this input.
    placeholders:
      session:
        username: "i.e. me@example.com"
        password: "i.e. zugzugblubgzugbug"
      user:
        email: "i.e. me@me.com"
        password: "i.e. mypassword"
        password_confirmation: "i.e. mypassword"
        username: i.e. TheDonut
        name: e.g. George Washington
        company_name: e.g. Evil Powers Ltd.
        street: e.g. Mysterialane 123
        city: e.g. Raccooncity
        zip: e.g. 424242
      worklog:
        summary: "e.g. * Finish feature 1."
      invoice:
        content: "e.g. * 10 hours á 100 € = 2500"
        total: "e.g. 2500"
        payment_info: "e.g. * Wire to: 2691929, Gerbank, 250000. Or Paypal: payments@zeitkit.com"
        payment_terms: "e.g. * Amount is due opon invoice receival."
        note: "e.g. * VAT-Number: 502123 or other important info in your country."
        discount: "i.e. 100"
      invoice_default:
        payment_info: "e.g. * Wire to: 2691929, Gerbank, 250000. Or Paypal: payments@zeitkit.com"
        payment_terms: "e.g. * Amount is due opon invoice receival."
        note: "e.g. * VAT-Number: 502123 or other important info in your country."
        vat: "e.g. 19.00"
        custom_css: "e.g. .colored-text { color: #333; }"
      client:
        name: e.g. George Washington
        company_name: e.g. Evil Powers Ltd.
        street: e.g. Mysterialane 123
        city: e.g. Raccooncity
        zip: e.g. 424242
        credit_block_reason: I.e. Invoices are late.
      expense:
        reason: e.g. Train ride 16/03/2013
      note:
        content: "e.g. * TODO get back with more details"
      product:
        charge: "i.e. 20"
