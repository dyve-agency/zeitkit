h1 Clients
- if current_user.clients.empty?
  .alert.alert-info
    = link_to "x", "#", class: "close", data: {dismiss: "alert"}
    span.intro Clients allows you to:
    ul
      li Keep track of whom you work with.
      li Take notes to keep track on progress to close more deals.
      li Create worklogs, and invoices

table.table.table-bordered.table-striped
  thead
    tr
      th Name
      th Hourly rate
      th Controls

  tbody
    - @clients.each do |client|
      tr
        td= link_to client.name, client
        td= with_currency client.hourly_rate
        td
          .btn-group
            = link_to client, class: "btn" do
              i.icon-zoom-in
              span Details
            = link_to new_user_worklog_path(client: client), class: "btn" do
              i.icon-plus
              span Worklog
            = link_to edit_client_path(client), class: "btn hidden-phone" do
              span Edit
            = link_to client, :confirm => 'Are you sure?', method: :delete,
              class: "btn hidden-phone" do
              span Delete
= link_to 'New client', new_user_client_path, class: "btn btn-primary"
