- if notices?
  .notices
    - if flash[:notice]
      .alert.alert-success
        a.close data-dismiss="alert" ×
        = flash[:notice]
    - if flash[:alert]
      .alert.alert-danger
        a.close data-dismiss="alert" ×
        = flash[:alert]
    - if flash[:error]
      .alert.alert-danger
        a.close data-dismiss="alert" ×
        = flash[:error]
